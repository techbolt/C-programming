#include <stdio.h>

int main()
{
    //These are some main data types.

    //Integer(int)
    int age = 40;

    //Decimal(double)
    double gpa = 3.7;

    //Character(char)
    char grade = 'A'; //stores a single character.
    char name[] = "Hello John"; //stores many characters or string.
}